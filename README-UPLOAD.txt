CUSTOM WORKOUT BUILDER FIX — SMALL UPDATE PACKAGE

Upload and replace these four files in the ROOT of your GitHub repository:
- index.html
- styles.css
- app.js
- service-worker.js

This corrected version adds a clearly visible CUSTOM WORKOUT BUILDER directly on the Protocol/Schedule page. Users can:
- Name a custom workout
- Add or remove exercises
- Assign it to one or more of the 14 schedule days while saving
- Edit schedule days later and select custom workouts
- Duplicate protected original templates
- Edit or delete custom workouts

It also retains the bottom-navigation scrolling fix and changes the service-worker cache version.

After committing to GitHub:
1. Wait 1–3 minutes for GitHub Pages to deploy.
2. Open the website in Safari, not only from the Home Screen icon.
3. Refresh the page once.
4. Fully close and reopen the installed Home Screen app.
5. If the old code still appears, remove the Home Screen shortcut and add it again. Your data normally remains unless Safari website data is cleared.
